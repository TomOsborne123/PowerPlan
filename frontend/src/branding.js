/**
 * Bump `?v=` here and in `frontend/index.html` (`<link rel="icon">`) when
 * `public/favicon.png` changes — favicon caching is very aggressive.
 */
export const FAVICON_PATH = '/favicon.png?v=7'
