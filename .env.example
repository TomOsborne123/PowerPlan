PORT=5001

# MySQL (docker-compose)
DB_HOST=db
DB_PORT=3306
DB_USER=root
DB_PASSWORD=powerplan
DB_NAME=energy_tariff

# When the UI is on Netlify and the API elsewhere, allow browser requests:
# CORS_ORIGINS=https://your-site.netlify.app
